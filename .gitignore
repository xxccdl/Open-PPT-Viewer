# 构建产物
#
# `.cargo/config.toml` 里可能把 target-dir 指到别处（见 README「给小硬盘腾地方」），
# 这条是兜底
/target/

# 本机私有配置：里面是绝对路径，换台机器就不成立
/.cargo/config.toml

# 打包产物与第三方引导器：都能重新生成，体积也大
# （安装包 15MB+，WebView2 引导器 1.8MB）
/packaging/out/
/packaging/windows/redist/

# 调试用的临时目录
/.tmp-deck/

# 老师自己的标注与板书（课件的旁挂文件，形如 `课件.pptx.oppv-annot.json`）：
# 属于使用者数据，不该跟着源码走
*.oppv-annot.json

# 编辑器与系统
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
